# Database URL for Neon/Postgres connection
DATABASE_URL=postgresql://username:password@hostname:port/database

# Cloudflare Workers environment variables
# Set these in wrangler.toml [vars] section or Cloudflare dashboard

# Rate limiting binding (configured in Cloudflare Workers dashboard)
# RATE_LIMITER - Cloudflare Rate Limiting API binding
# This should be configured through the Cloudflare dashboard under Workers settings

# Development environment variables (optional)
# NODE_ENV=development