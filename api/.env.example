# Database URL for Neon/Postgres connection
DATABASE_URL=postgresql://username:password@hostname:port/database

# Cloudflare Workers environment variables (set in wrangler.toml or CF dashboard)
# These are examples - actual values should be set in production environment